package com.tinslam.battleheart.interfaces;

/**
 * An interface that contains methods that need to be initialized when instantiating a Button.
 */
public interface ButtonInterface{
    boolean onDown();
    boolean onUp();
}
